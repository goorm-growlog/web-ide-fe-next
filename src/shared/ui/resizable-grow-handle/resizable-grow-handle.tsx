import { ResizableHandle } from '../shadcn/resizable'

const ResizableGrowHandle = () => {
  return (
    <ResizableHandle className="cursor-col-resize bg-border transition-all duration-200 ease-out hover:w-1.5" />
  )
}

export default ResizableGrowHandle
