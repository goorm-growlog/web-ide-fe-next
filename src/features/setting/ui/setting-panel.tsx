import { PANEL_BASE_STYLES } from '@/shared/constants/ui'
import { cn } from '@/shared/lib/utils'

const SettingsPanel = () => (
  <div className={cn(PANEL_BASE_STYLES)}>Settings</div>
)

export default SettingsPanel
